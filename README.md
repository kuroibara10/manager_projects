# manager_projects

## Installation

1 - Install ruby --go terminal--> ruby -v;
2 - Chek in terminal gem-v;
3 - Install sintara : gem install sinatra then gem install sinatra-contrib
4 - Chek in terminal version sintara : gem list sinatra
5 - Dtabases -> Install Sqlite3 : gem install sqlite3 --platform ruby
6 - Chek in terminal version sqlite3: sqlite3 --version
7 - To Encrypting passwords using bcrypt : gem install bcrypt & gem install bcrypt --platform=ruby

## Usage

Run project:

```bach
ruby app.rb
```

Then in your search browser : http://localhost:8080/

run database:

```bach
sqlite3 mydatabase.db
Show tables database: .tables
Show structure table ex users: .schema users
exit : .exit
```

## Team

Created by Abdillah Bara

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Manager Projects - Sing up</title>
    <link rel="stylesheet" href="css/style.css" />
    <link
      rel="shortcut icon"
      href="assests/logo.png"
      type="image/x-icon"
    />
  </head>
  <body>
    <div class="singUp-page">
        <a href="/"><img src="assests/logo.png" alt="COLLABORATE" class="img-singUp"/></a>
        <h1 class="head-home">Sing up</h1>
        <% if @message %>
          <% if @type == "success" %>
            <%# <div style="padding: 10px; background: #d4edda; color: #155724; border-radius: 5px; margin-bottom: 15px;"> %>
            <div style="color: #155724;">
              <%= @message %>
            </div>
          <% else %>
            <%# <div style="padding: 10px; background: #f8d7da; color: #721c24; border-radius: 5px; margin-bottom: 15px;"> %>
            <div style="color: #721c24;">
              <%= @message %>
            </div>
          <% end %>
        <% end %>
        <form action="/sing_up" class="form-rl" method="POST">
            <div id="lable-name">
                <label for="username">Username</label>
                <input name="username" type="text" required/>
            </div>
            <div id="lable-email">
                <label for="email">Email</label>
                <input name="email" type="email" required/>
            </div>
            <div id="lable-password">
                <label for="password">Password</label>
                <input name="password" type="password" required/>
            </div>
            <div id="lable-password-conf">
                <label for="password_conf">Password confirmation</label>
                <input name="password_conf" type="password" required/>
            </div>
            <input type="submit" class="btns" value="Sing up">
            <p>Already have an account? <a href="/login">Log in</a></p>
        </form>
    </div>
  </body>
</html>
